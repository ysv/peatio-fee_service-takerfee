RSpec.describe Peatio::FeeService::Takerfee do
  it "has a version number" do
    expect(Peatio::FeeService::Takerfee::VERSION).not_to be nil
  end

  xit "does something useful" do
    expect(false).to eq(true)
  end
end
